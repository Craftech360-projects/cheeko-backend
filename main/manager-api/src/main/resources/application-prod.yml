knife4j:
  production: false
  enable: true
  basic:
    enable: false
    username: renren
    password: 2ZABCDEUgF
  setting:
    enableFooter: false
spring:
  datasource:
    druid:
      #MySQL
      # driver-class-name: com.mysql.cj.jdbc.Driver
      # # Railway MySQL Configuration - Updated for Railway database
      # url: jdbc:mysql://yamanote.proxy.rlwy.net:16958/railway?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true&useSSL=true&allowPublicKeyRetrieval=true
      # username: root
      # password: cTefiuLmZSMqGpcQImUmBIsUBhNyUvrK
      driver-class-name: com.mysql.cj.jdbc.Driver
      # Railway MySQL Configuration - Updated for Railway database
      url: jdbc:mysql://nozomi.proxy.rlwy.net:25037/railway?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&nullCatalogMeansCurrent=true&useSSL=true&allowPublicKeyRetrieval=true
      username: root
      password: OcaVNLKcwNdyElfaeUPqvvfYZiiEHgdm

      # Release Liquibase lock on startup
      connection-init-sqls:
        - "UPDATE DATABASECHANGELOGLOCK SET LOCKED=0, LOCKGRANTED=NULL, LOCKEDBY=NULL WHERE ID=1"

      initial-size: 10
      max-active: 100
      min-idle: 10
      max-wait: 6000
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      stat-view-servlet:
        enabled: false
      filter:
        stat:
          log-slow-sql: true
          slow-sql-millis: 1000
          merge-sql: false
        wall:
          config:
            multi-statement-allow: true
  data:
    redis:
      # Railway Redis Configuration
      host: maglev.proxy.rlwy.net # Railway Redis server address
      port: 16780 # Railway Redis server connection port
      password: beVfOKcZjMUCBiZIQnvSMivhpSeijpKP # Railway Redis server connection password
      username: default # Railway Redis username
      database: 0 # Redis database index (default 0)
      timeout: 30000ms # Connection timeout (milliseconds) - increased timeout
      ssl:
        enabled: false # Try to disable SSL, Railway may not require it
      lettuce:
        pool:
          max-active: 8 # Maximum connections in pool (use negative value for unlimited)
          max-idle: 8 # Maximum idle connections in pool
          min-idle: 0 # Minimum idle connections in pool
        shutdown-timeout: 100ms # Client graceful shutdown wait time

  # Liquibase configuration for migrations
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true
    drop-first: false